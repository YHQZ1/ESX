# Used by docker-compose.yml at the root
ENVIRONMENT=development

# --- PostgreSQL (Shared Instance) ---
POSTGRES_USER=esx
POSTGRES_PASSWORD=esx
POSTGRES_DB=postgres

# --- Redis ---
REDIS_PASSWORD=local_redis_password

# --- Global Security ---
JWT_SECRET=super_secret_local_dev_key_do_not_use_in_production